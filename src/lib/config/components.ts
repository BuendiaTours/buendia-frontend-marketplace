/**
 * Configuración global por defecto de componentes de la aplicación
 *
 * Este archivo centraliza las opciones por defecto de los componentes.
 * Los valores aquí definidos se pueden sobrescribir al invocar cada componente.
 */

/**
 * Configuración por defecto para PhotoSwipe (lightbox y gallery)
 */
export const photoSwipeDefaults = {
	showHideAnimationType: 'none' as const,
	bgOpacity: 0.9,
	wheelToZoom: true
};

// Aquí puedes añadir más configuraciones de otros componentes
// export const otroComponenteDefaults = { ... };
